# Helper for row names or fallback names

Helper for row names or fallback names

## Usage

``` r
rownames_or_names(x, fallback = NULL)
```

## Arguments

- x:

  Matrix.

- fallback:

  Fallback names.

## Value

Character vector.
