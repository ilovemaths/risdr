% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sdr_kernels.R
\name{compute_dr}
\alias{compute_dr}
\title{Compute Directional Regression}
\usage{
compute_dr(
  X,
  y,
  Sigma = NULL,
  nslices = 6,
  slice_type = c("quantile", "equal_width"),
  stabilize_slices = TRUE,
  stabilization = c("eigenfloor", "ridge", "nearest_pd"),
  eps = 1e-08
)
}
\arguments{
\item{X}{Numeric predictor matrix.}

\item{y}{Numeric response vector.}

\item{Sigma}{Covariance matrix used for standardisation.}

\item{nslices}{Number of response slices.}

\item{slice_type}{Slicing strategy.}

\item{stabilize_slices}{Logical. Stabilise slice covariance matrices.}

\item{stabilization}{Stabilisation method.}

\item{eps}{Eigenvalue floor.}
}
\value{
A list containing DR kernel, eigenvalues, directions, scores, and slices.
}
\description{
Computes Directional Regression using the canonical pairwise-slice
formulation.
}
\details{
Directional Regression is computed using the pairwise-slice formulation.

\deqn{
M_{DR} =
\sum_h \sum_k
p_h p_k
(2I_p - A_{hk})
(2I_p - A_{hk})^\top
}

where

\deqn{
A_{hk} =
\Sigma_h + \Sigma_k +
(\mu_h - \mu_k)
(\mu_h - \mu_k)^\top
}
}
\examples{
X <- as.matrix(mtcars[, c("disp", "hp", "wt")])
fit <- compute_dr(X, y = mtcars$mpg, nslices = 4)
head(fit$eigenvalues)
}
