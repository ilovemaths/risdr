% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covariance_estimators.R
\name{cov_mec}
\alias{cov_mec}
\title{Maximum Entropy Covariance estimator}
\usage{
cov_mec(
  X,
  y,
  nslices = 6,
  response_type = c("continuous", "categorical", "survival"),
  delta = NULL,
  alpha = NULL,
  eps = 1e-06,
  ...
)
}
\arguments{
\item{X}{Numeric matrix or data frame.}

\item{y}{Numeric response vector.}

\item{nslices}{Number of response slices.}

\item{response_type}{Response type. One of \code{"continuous"}, \code{"categorical"},
or \code{"survival"}.}

\item{delta}{Optional event indicator for survival data.}

\item{alpha}{Convex weight assigned to the entropy-maximising slice
covariance. If NULL, alpha is chosen adaptively from the condition number
of the pooled covariance.}

\item{eps}{Small positive eigenvalue floor used for log-determinant stability.}

\item{...}{Additional arguments passed to internal methods.}
}
\value{
A covariance matrix with attributes containing the shrinkage weight
and entropy diagnostics.
}
\description{
Computes a maximum-entropy motivated covariance estimator for SDR.
}
\details{
The estimator uses response slicing to obtain slice-specific covariance
matrices, selects the covariance matrix with maximum log-determinant entropy,
and combines it with the pooled covariance matrix through convex shrinkage.

For censored survival responses, the function accepts \code{delta} and applies
a survival-aware response construction before slicing.
}
\examples{
X <- as.matrix(mtcars[, c("disp", "hp", "wt")])
Sigma <- cov_mec(X, y = mtcars$mpg, nslices = 4)
attr(Sigma, "alpha")
attr(Sigma, "entropy_slice")
}
